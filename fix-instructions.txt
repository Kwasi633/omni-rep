// Please add this fix to the reputation-view.tsx file:
// Change the import from Lucide to:
import {
  Activity,
  Award,
  CheckCircle,
  Clock,
  Copy,
  Download,
  ExternalLink,
  Eye,
  Github,
  QrCode,
  Share,
  Shield,
  TrendingUp,
  UserCheck,
  Users,
  Wallet,
  X,
} from "lucide-react";

// And before each JSX usage of the Lucide icons, add // @ts-ignore
// For example:
// @ts-ignore
<Shield className="h-12 w-12 text-gray-400 mx-auto mb-4" />

// Similarly, for Recharts components:
// @ts-ignore
<ResponsiveContainer width="100%" height="100%">
  // @ts-ignore
  <RadarChart data={radarData}>
    // @ts-ignore
    <PolarGrid stroke="#374151" />
    // @ts-ignore
    <PolarAngleAxis dataKey="subject" tick={{ fill: "#D1D5DB", fontSize: 12 }} />
    // ...etc
  </RadarChart>
</ResponsiveContainer>

// Finally, fix the Progress component issue:
// Check if the component is properly typed to accept a value prop.
// You may need to update the Progress component definition if it doesn't support the value prop.
